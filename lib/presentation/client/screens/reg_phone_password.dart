import 'package:flutter/material.dart';

class RegPhonePassword extends StatefulWidget {
  const RegPhonePassword({super.key});

  @override
  State<RegPhonePassword> createState() => _RegPhonePasswordState();
}

class _RegPhonePasswordState extends State<RegPhonePassword> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('hello')));
  }
}
